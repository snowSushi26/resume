{
  "name": "resume",
  "version": "0.1.0",
  "repository": "git@github.com:snowSushi26/resume.git",
  "license": "MIT",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "rm -rf docs && parcel build src/index.html --dist-dir docs --public-url /resume/ --no-source-maps --no-cache"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
